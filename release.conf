# Values ship reads. The prose is in RELEASING.md; the loop is in
# ~/notes/releasing.md. Format and keys: anderix/ship, docs/PROFILE.md.
loop=cargo-dist
version_file=Cargo.toml
apt_package=xray
deb_packages=1
deb_arches=amd64 arm64
crates=x-ray
homebrew=yes
winget=Excelano.xray:x-ray-x86_64-pc-windows-msvc.zip
store_lanes=
